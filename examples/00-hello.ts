/**
 * Placeholder sample, so the examples project has an input to type-check against the built types.
 * The runnable samples — log in, list devices, listen for events, drive a camera — land with the
 * library source and replace this file.
 */
import * as sdk from "../dist/index.js";

console.log(`eufy SDK public exports: ${Object.keys(sdk).length}`);
